1. Find the changes made since the last update use `git diff`
2. Store the current hash at the end of `README.md` for next run
3. If no hash is prsent, assume last 5 commits. 
4. Read and update `README.md` based on the diff.
